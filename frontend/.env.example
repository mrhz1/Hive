# Base URL of the FastAPI service.
VITE_API_BASE_URL=http://localhost:8100

# Local stand-in for the platform-authenticated principal. On Cloudera AI
# the identity arrives from the platform, so this is unset there and the
# app reads whoever the API says the caller is.
VITE_DEV_USER_ID=
